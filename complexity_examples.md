# Time complexity examples

## single loop
a loop running n times has O(n) complexity

##nested loop
two loops each running n times have O(n^2) complexity.

## function call
if a function has a loop of n , calling it once gives O(n).

